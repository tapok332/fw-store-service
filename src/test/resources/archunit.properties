# store-service has no Kafka consumers (producer-only), so the kafka-listener
# layering rule legitimately matches zero methods. Allow an empty should rather
# than editing the shared HexagonalArchitectureTest template (ADR 0014).
archRule.failOnEmptyShould=false
